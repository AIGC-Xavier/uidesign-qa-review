---
name: uidesign-qa-review
description: "对照设计稿检查开发页面、截图或前端源码的视觉还原情况，输出附证据、代码位置和修正方向的问题清单。适用于 UI 走查、Figma 与实现比对、前端样式审查和设计验收；支持设计稿与源码仓库、页面链接或实现截图组合输入。"
---

# UI 设计走查

把视觉差异整理成开发可以复核和修正的任务。以用户指定的设计版本为基准；走查默认只读取页面、源码和设计，不修改实现或设计稿。用户同时要求修复时，再在授权范围内修改并复查。

根据输入选择检查方式：

- **设计稿＋实现截图**：对齐图像条件后比较可见差异。
- **设计稿＋页面链接**：访问页面，取得对应状态的截图后比较。
- **设计稿＋前端源码**：定位实际渲染组件和样式来源，对照设计属性；具备运行条件时再用浏览器确认最终表现。

三种方式可以组合。源码用于解释视觉偏差和定位修复点，截图用于验证实际呈现，二者不能互相替代。

## 1. 建立可比较的证据

先确认哪份材料是设计、哪份是实现，以及本次涉及的页面、主题、语言和状态。不要仅凭截图顺序判断来源。已有信息足够时直接开始，仅询问影响判断的缺项。

有页面链接时，使用可用的浏览器或设计工具访问并截取对应区域。固定视口、缩放与状态，等字体和图片完成加载后再比较。把设计节点或截图名称与实现 URL、截图名称对应记录下来。无法访问的材料列为未检查，不以描述代替实际观察。

只有截图时，先判断两图是否同尺度、同裁切范围；对拍照图、压缩图和不同像素密度的截图，不给出未经验证的精确色值或像素差。如果材料是拍摄屏幕的照片，应考虑拍摄角度、环境光和相机处理造成的色差，适当降低颜色判断的精度；轻微色差需用直接截图或样式属性复核后再确认为问题。

## 2. 从整体到组件检查

确定设计与实现的对应关系后，从页面骨架逐步检查到局部样式。以下清单适用于截图、实际页面及源码比对；对每个适用项核对，不将无法取得证据的项目视为通过。比较时保持组件用途与状态一致，优先识别影响多个位置的共同原因。

### 页面结构与对齐关系

- 核对区域划分、排列方向、列数及首屏位置，确认整体结构与设计对应。
- 检查容器宽度、各栏占比和内容边界，避免局部尺寸正确但整体比例偏离。
- 对照元素的左、右、上、下边缘，以及中心线和文字基线，定位水平或垂直偏移。
- 检查并列项目的分布规则；设计采用等距排列时，确认各处间隔一致。

### 控件尺寸与空间分配

- 对照按钮、输入框、卡片和列表项的宽高及最小尺寸，同用途组件采用一致的尺寸规则。
- 分别检查上下元素的间隔和左右元素的间隔，避免只核对一个方向。
- 核对内容区距左右边界的留白，以及设计规定的上下边距。
- 检查卡片、列表项等容器内部四边的留白，区分容器内边距与相邻元素间距。

### 字体层级与文字编排

- 将标题、正文和辅助文字分组对照，分别核验字体、字号和字重，避免用一种文字层级推断其他层级。
- 检查行高、段落间隔及多行文字的疏密程度，区分内容长度变化与排版规则偏差。
- 核对左对齐、居中、右对齐等文字排列方式，以及换行、截断和可用宽度约束。

### 色彩与填充

- 分别核对页面背景、分区背景及卡片表面颜色。
- 对标题文字、正文文字和辅助文字分别检查颜色及透明度，确认信息层级没有变淡或变重。
- 对照图标用色、分割线和边框颜色，检查与相邻背景的关系。
- 检查按钮、标签等组件的填充色，并在对应的交互状态和主题下比较。
- 可读取设计属性与实现样式时记录数值；只有视觉证据时注明估计，不虚构精确色值。

### 边角形态

- 分别核对卡片、按钮、输入框及图片容器的圆角，避免所有组件共用一个不适用的半径。
- 检查四角是否使用相同或指定的不同圆角，以及裁切后的内容是否遵循容器边角。
- 同时观察圆角与组件尺寸形成的形态，区分普通圆角、胶囊形和圆形控件。

### 叠放顺序与表面效果

- 检查元素、浮层和遮罩的前后关系，确认没有意外遮挡或穿透显示。
- 对照阴影的方向、偏移、模糊范围、扩散及透明度，判断视觉层次是否一致。
- 检查毛玻璃或背景模糊的作用区域、强度，以及遮罩的颜色和透明度。

### 图标的视觉规格

- 检查图标容器尺寸、图形实际占用范围及与相邻文字的对齐，不因图形主题不同就判定偏差。
- 对照描边宽度和视觉粗细，留意缩放后线条是否明显过重或过轻。
- 按前述色彩规则核对图标颜色及透明度，并保持状态一致。

上述维度也用于焦点、悬停、禁用、加载、异常及浮层等状态的视觉对照；具体覆盖范围按第 4 节记录。

文字措辞、用户数据、照片主题和占位素材的不同不作为问题。图片容器的比例、裁切方式和对齐仍属于视觉检查。若不同内容使布局无法直接比较，标记需要等价内容复核，不直接归因于样式错误。

用户提供设计规范、Design Token 文档或组件库时，同时检查实现是否符合其中适用的规则。发现不符合项，在报告中标注具体规范条目或变量名称，并记录预期值与实现值；不能只笼统写“不符合规范”。设计与规范冲突时呈现冲突，不擅自选择另一个版本作为标准。

## 3. 前端源码与设计稿比对

用户提供源码目录、仓库或组件文件时，按以下步骤检查。先读取适用的项目说明，确认技术栈、页面路由、启动方式和样式组织；搜索范围从目标页面及其依赖开始，不无目的扫描整个仓库。

### 建立设计到代码的对应关系

从设计节点或画面区域定位路由、页面组件、复用组件和样式文件。记录“设计区域 → 组件 → 样式或变量”的映射，确认组件确实被目标页面引用；同名但未使用的文件、废弃页面和构建产物不能当作当前实现。

读取设计工具可提供的尺寸、布局约束、字体、间距、圆角、颜色及设计变量。若只有设计截图，明确哪些值是估计，不能据此声称代码与设计存在精确数值冲突。

### 追踪最终生效的样式

结合项目实际使用的方式检查 CSS、Sass/Less、CSS Modules、工具类、CSS-in-JS、组件属性和主题变量，不假定某一种框架。重点核查：

| 对照项 | 源码检查重点 |
| --- | --- |
| 结构与布局 | 容器层级、Flex/Grid、宽高约束、对齐、间隔、绝对定位、溢出和滚动 |
| 组件尺寸 | 内外边距、边框、盒模型、最小尺寸以及按钮或输入框的尺寸变体 |
| 字体与颜色 | 字体加载、继承、字号、字重、行高、透明度、变量引用和主题覆盖 |
| 自适应 | 媒体查询、容器查询、断点、百分比、rem/em、视口单位和缩放条件 |
| 视觉状态 | 控件属性、条件类名、伪类、状态分支、浮层层级和浅深色变量 |

遇到多个声明时，沿导入关系、选择器优先级、声明顺序、继承、内联样式和条件分支查明实际适用规则。不要把搜索到的一条声明直接当作最终样式。设计中的填充容器、自适应尺寸等约束应与实现行为比较，不能机械要求写成同一个固定像素值。

### 用运行结果确认

有运行条件时，按项目既有方式启动或复用开发预览，记录视口、主题、页面状态与所审查版本。通过浏览器截图、元素边界和计算样式核对源码结论；必要时覆盖目标断点与对应状态。不要仅为检查而更改依赖版本、锁文件或业务数据。

无法运行时仍可完成静态比对，但必须区分：

- **渲染确认**：设计依据、代码来源和浏览器实际表现相互对应。
- **源码确认**：能确认设计属性与适用代码规则冲突，但尚未验证最终画面。
- **待验证**：受运行时数据、样式覆盖、字体加载、断点或访问条件影响，暂不能确定。

只有源码确认的问题不能写成已观察到的视觉缺陷；待验证项不计入确认问题数量。整体视觉评价只针对实际看到的范围。

### 给开发的定位信息

源码模式下，每条问题除视觉证据外，还应注明文件路径与行号、组件名、选择器或变量名、适用断点和状态，以及设计值、代码值或对应约束。无法获得准确数值时写明限制。

修正建议优先指向实际控制该效果的父容器、共享组件或主题变量，并说明影响范围，避免用局部硬编码掩盖公共规则问题。需要代码示例时给出最小建议片段并标记为建议，不声称已经应用。用户要求直接修复时，修改后检查相关页面、主题和状态，并回报实际验证结果。

## 4. 处理多状态与覆盖范围

仅在用户要求多状态走查或材料包含状态稿时建立覆盖表。按页面、主题、状态分别记录“已对照”“仅设计可见”“仅实现可见”“测试条件不足”。不要把默认态检查推广成整个流程通过。

按页面或组件的实际功能覆盖默认、焦点、悬停、选中、展开/收起、禁用、加载、空数据、错误、重试和浮层打开/关闭等适用状态。状态依赖特定数据、权限或外部服务时，准确记录测试条件与阻塞点；演示数据和原型连线不能证明真实服务工作正常。

这项 skill 的结果是视觉还原报告。功能正确性、无障碍、业务流程与设计改稿只有在用户另行要求时才扩展，并与视觉问题区分。

## 5. 将观察转成可执行问题

每条确认的问题使用稳定编号，并包含：

- **位置与状态**：页面、区域、主题及触发状态。
- **证据**：对应设计和实现的截图、裁切区域或可访问链接。
- **确认方式**：渲染确认或源码确认；未能确认的项单独列为待验证。
- **代码定位**：提供源码时记录文件与行号、组件和样式来源；没有源码时不编造。
- **预期与实际**：分别描述两侧观察，不把推测写成事实。
- **修正方向**：指出需调整的属性、组件或父容器；只有读取到可靠数值时才给精确值。
- **影响程度**：高、中或低，并用一句话说明影响。

高：主要结构或关键控件明显偏离，造成遮挡、截断或识别困难。中：局部层级、尺寸或对齐出现稳定可见的偏差。低：范围有限的细节偏差，不妨碍主要信息识别。按影响判断，不仅按相差多少像素判断。

同一组件或同一规则在多处出错时合并为一条，标注“全局性问题”，列出已确认受影响的位置及共同原因，避免重复计数；不要把局部观察推断成所有页面都有问题。暂时无法确认的观察进入“待验证”，不计入确认问题数量。问题清单只列确认存在偏差的问题，不逐项列出“检查通过”的维度；已检查覆盖范围另行汇总。

## 6. 交付与复核

报告开头说明本次基准、检查环境、覆盖范围、确认问题总数和高/中/低分布。源码模式额外记录审查的分支或提交（可获取时）、工作区是否含未提交修改、是否运行预览，以及渲染确认与源码确认各有多少项。随后按影响程度排列问题，最后列出阻塞与待验证项。没有统一量化方法时，不编造还原率百分比。

按用户要求输出 Markdown、PDF 或 Word。需要可独立转发的文档时，将证据图嵌入文件，不依赖聊天缓存或本机图片路径。导出后检查图片可见、文字可读、链接与问题编号匹配。

复查时沿用问题编号，对照新的实现记录已修正、仍存在或无法复测。未完成检查的状态必须保留说明。
